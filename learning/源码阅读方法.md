- 参考文档《leveldb实现解析》
- 先会用，观察API；再往里钻
- 带问题去读
  1. 为什么存储数据的文件夹中有好几种类型的文件，为什么这么设计，去读源码
  2. 在写入put时都会有哪些动作，在内存写满后怎么写到磁盘里；
  3. get()时，会发生什么？先检查内存；如果内存没有去磁盘上找，它是如何确定哪个磁盘文件中有这个key值；如果找的时候有一个后台进程正在合并这些磁盘文件，这时候查找是如何进行的？是等待合并完（I/O操作）还是什么操作？如何保障高效率的查询？
  4. 批量写又是怎样的逻辑呢？
  5. 删除一个key是怎么实现的？是真的删除了还是怎么处理？
  6. 是怎么实现扫描整个库，如果正在读的时候有进程写入数据，怎么办？是加锁吗？leveldb是不加锁的，它能保障读不会读到正在写入的数据，这是怎么实现的呢？什么是多版本？是如何通过不可变性解决多版本问题的？
  7. 打开数据库的时候需要做什么？
- 切入点，写入
- 不断做知识屏蔽才能读懂
- 
